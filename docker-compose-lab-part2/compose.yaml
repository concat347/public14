services:
  web:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
    volumes:
      - redis_data:/data  # Persists data to a managed volume

volumes:
  redis_data:           # Defines the volume at the project level
